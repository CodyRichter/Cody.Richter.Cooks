---
alwaysApply: true
---

- This repo contains a React frontend, a Python backend, and is using AWS CDK for the infrastructure.
- The React frontend is in: "client/"
- The Python backend is in: "server/src"
- The AWS infrastructure is in: "server/cdk"
